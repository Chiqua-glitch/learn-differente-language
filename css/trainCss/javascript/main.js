/*const headerBtn = document.querySelectorAll("header a");

headerBtn.forEach(element => {
    element.addEventListener("click", () => {
        for (let i = 0; i < element.parentNode.childNodes.length; i++) {
            if (element.parentNode.childNodes[i].className == "none menu") {
                element.parentNode.childNodes[i].className = "menu";
            } else if (element.parentNode.childNodes[i].className == "menu") {
                element.parentNode.childNodes[i].className = "none menu";
            }
        }
    })
})*/